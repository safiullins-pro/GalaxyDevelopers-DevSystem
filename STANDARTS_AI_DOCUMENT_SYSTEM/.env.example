# ==============================================
# DocumentsSystem - Environment Variables Template
# ==============================================
# ВАЖНО: Скопируйте этот файл в .env и заполните реальными значениями
# НЕ КОММИТЬТЕ .env файл в git репозиторий!

# Database Configuration
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=developer_control
POSTGRES_USER=control_admin
POSTGRES_PASSWORD=YOUR_SECURE_PASSWORD_HERE  # CHANGE THIS!

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=YOUR_REDIS_PASSWORD_HERE  # CHANGE THIS!

# Telegram Bot Configuration (Optional)
# Получите токен у @BotFather в Telegram
TELEGRAM_BOT_TOKEN=  # Leave empty if not using
TELEGRAM_CHAT_ID=    # Your Telegram chat ID

# Security Keys (Generate strong random values!)
JWT_SECRET_KEY=GENERATE_STRONG_RANDOM_KEY_HERE  # Use: openssl rand -hex 64
ENCRYPTION_KEY=GENERATE_STRONG_KEY_HERE         # Use: openssl rand -hex 32

# Application Settings
ENVIRONMENT=development  # development, staging, production
LOG_LEVEL=INFO          # DEBUG, INFO, WARNING, ERROR, CRITICAL
DEBUG=false

# API Configuration
API_RATE_LIMIT=100
API_RATE_LIMIT_WINDOW=60

# Performance Settings
DB_POOL_MIN_CONNECTIONS=3
DB_POOL_MAX_CONNECTIONS=10
REDIS_POOL_MAX_CONNECTIONS=20

# Monitoring
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=CHANGE_THIS_PASSWORD  # CHANGE THIS!

# AI API Keys (if using AI features)
OPENAI_API_KEY=sk-...  # Your OpenAI API key
ANTHROPIC_API_KEY=sk-ant-...  # Your Anthropic API key

# Git Integration (if using)
GIT_REPO_URL=https://github.com/your-org/your-repo.git
GIT_TOKEN=ghp_...  # Your GitHub personal access token
GIT_BRANCH=main
GIT_USER_NAME=System Bot
GIT_USER_EMAIL=bot@example.com

# Compliance Settings
AUDIT_LOG_RETENTION=2555  # days
TASK_LOG_RETENTION=365    # days
GDPR_ENABLED=true
SOX_COMPLIANCE=true

# ==============================================
# ИНСТРУКЦИЯ ПО ГЕНЕРАЦИИ БЕЗОПАСНЫХ ПАРОЛЕЙ:
# ==============================================
# 1. Для генерации паролей используйте:
#    - Linux/Mac: openssl rand -base64 32
#    - Python: python -c "import secrets; print(secrets.token_urlsafe(32))"
#    - Online: https://passwordsgenerator.net/
#
# 2. Для JWT Secret Key:
#    openssl rand -hex 64
#
# 3. Для Encryption Key:
#    openssl rand -hex 32
#
# НИКОГДА НЕ ИСПОЛЬЗУЙТЕ:
# - Простые пароли (123456, password, admin и т.д.)
# - Пароли из примеров или документации
# - Одинаковые пароли для разных сервисов
# ==============================================